export interface ClockSettings {
  enabled: boolean;
  shortcut: string;
  autoHideSeconds: number;
  fontSize: string;
  showDate: boolean;
  showSeconds: boolean;
  clockColor: string;
  blinkColon: boolean;
}
