{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "calendar-editor",
  "description": "Capability for the calendar event editor overlay window",
  "windows": ["calendarEditor"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-destroy",
    "core:window:allow-start-dragging"
  ]
}
