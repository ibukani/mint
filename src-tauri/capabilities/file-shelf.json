{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "file-shelf",
  "description": "Capability for the file shelf overlay window",
  "windows": ["fileShelf"],
  "permissions": [
    "core:default",
    "dialog:allow-open",
    "opener:default",
    "opener:allow-open-path",
    "drag:default"
  ]
}
