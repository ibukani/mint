{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "clock",
  "description": "Capability for the clock overlay window",
  "windows": ["clock"],
  "permissions": [
    "core:default",
    "core:window:allow-hide",
    "core:window:allow-destroy",
    "core:window:allow-start-dragging"
  ]
}
